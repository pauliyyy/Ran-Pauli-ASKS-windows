# Playbook Index

个人 playbook 索引（开源模板占位）。条目格式：`## 标题` + `**触发词**：「...」`，以 `---` 分隔。
测试依赖的合成条目见下。

---

## DSH 摄入

**触发词**：「用 DSH 摄入 inbox」

使用 API DSH。

---

## 摄入

**触发词**：「摄入 inbox」

使用 ingest_paper.py。

---

## 更新工程文档

**触发词**：「更新工程文档」

先判断改动类型。

---

## 班子会提案

**触发词**：「班子会提案」

转交 projects/科研副主任工作区，走 workspace proposal 流程（不触发会议纪要摄入）。
