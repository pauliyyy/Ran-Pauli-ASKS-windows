# ASKS 运行依赖（Python ≥ 3.10）
#
# 安装：pip install -r requirements.txt
#
# 说明：
# - 核心知识系统（摄入/查询/图/Hub）：PyYAML、numpy、tiktoken、scipy
# - PDF 摄入：PyMuPDF（fitz）；Docling 引擎可选（另建 .venv-docling）
# - PPTX 视觉复核链路：python-pptx、Pillow；另需系统安装 LibreOffice（soffice）
# - Excel/旧格式文档摄入：openpyxl、xlrd（读 .xls）、xlwt（写 .xls 兼容）
# - BM25 基线实验（.scripts/baseline_*.py）：rank-bm25

PyYAML>=6.0
numpy>=1.26
tiktoken>=0.7
scipy>=1.11
PyMuPDF>=1.24
python-pptx>=1.0
Pillow>=10.0
openpyxl>=3.1
xlrd>=2.0
xlwt>=1.3
rank-bm25>=0.2.2

# ---- 可选（按需） ----
# Docling 摄入引擎（PDF 高级布局提取）：pip install docling
#   extractor.py 会自动查找 .venv-docling/bin/python（Unix）
#   或 .venv-docling/Scripts/python.exe（Windows），详见 README
